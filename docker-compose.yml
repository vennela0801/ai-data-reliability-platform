services:

  ai-reliability-platform:

    build:
      context: .
      dockerfile: docker/Dockerfile

    container_name: ai_reliability_platform

    ports:
      - "8501:8501"

    volumes:
      - ./warehouse/database:/app/warehouse/database

    env_file:
      - .env
